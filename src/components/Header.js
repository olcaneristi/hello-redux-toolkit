import React from "react";
import Form from "./Form";

const Header = () => {
  return (
    <header className="header">
      <h1>Todos</h1>
      <Form />
    </header>
  );
};

export default Header;
